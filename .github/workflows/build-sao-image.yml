name: Build SAO Image

on:
  workflow_dispatch:

env:
  PRODUCTION_IMAGE_NAME: ghcr.io/jbcupps/sao
  PRODUCTION_DOCKERFILE: docker/Dockerfile

jobs:
  build-and-push:
    name: Build and Push Production GHCR Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_PAT != '' && secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push production app image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ env.PRODUCTION_DOCKERFILE }}
          push: true
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.description=SAO Rust server image
            org.opencontainers.image.licenses=MIT
          tags: |
            ${{ env.PRODUCTION_IMAGE_NAME }}:latest
            ${{ env.PRODUCTION_IMAGE_NAME }}:${{ github.sha }}
