[package]
name = "skill-azure-bootstrap-troubleshooting"
version = "0.1.0"
edition = "2021"
description = "Structured Azure bootstrap diagnostics and guided recovery suggestions"

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
